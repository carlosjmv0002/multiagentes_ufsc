/*
    Smart Home - Versao de Teste (Sem GUI)
    ---------------------------------------
    
    Esta versao executa testes automaticos dos 3 cenarios
    sem necessidade de interface grafica.
*/

mas smart_home_test {

    // Security agents
    agent camera: camera.asl
    agent fechadura: fechadura.asl

    // Climate control agents
    agent ar_condicionado: ar_condicionado.asl

    // Lighting control agents
    agent lampada: lampada.asl
    agent cortina: cortina.asl
    
    // Test agent
    agent teste: teste_automatico.asl

    workspace casa {
      // Artifacts are created by agents during initialization
    }
}
